name: Run python
on:
  push:
  schedule:
    - cron: "0 8 * * *"
jobs:
  build:
    name: envl
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run my python
      run: 
        python test.py